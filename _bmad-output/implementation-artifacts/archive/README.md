# Archive — story/spec cũ (trước v1.5.1)

Các file ở đây là story đã hoàn thành và spec release cũ (v1.2.0 → v1.5.0), archive ngày 2026-06-04 để root `implementation-artifacts/` chỉ còn story v1.5.1 đang active.

- Numbered story `1-1`…`7-1`: vẫn được track trong `../sprint-status.yaml` (đa số `done`).
- `spec-*.md`: spec từng feature release cũ, giữ làm tham chiếu lịch sử.
- `review-*.md`, `deferred-work.md`, `spec-...-mvp.md`: artifact review/MVP cũ.

Không xóa — chỉ dời. Cần tra cứu lịch sử implement thì vào đây.
